# Cockpit

## 현재 상황

Cockpit은 사람이 손으로 쓴 `PROGRESS.md` 하나를 로컬에서 결정론적으로 읽어 브라우저에 보여주는 읽기 전용 뷰어다. 지도가 첫 화면의 기준이며, 현황·다음·제약은 평문 개요로 읽힌다. 뜻이 다른 유효 문서에서도 첫 화면 읽기가 그대로 됨이 확인되어 닫혔고 막는 제품 결함은 없었다. 다음 과제는 사용자 선택 전이다.

## 다음 전환

다른 문서 읽힘 확인이 끝난 상태 → 다음 과제가 정해지기 전 상태. 사용자가 다음 과제를 정하면 닫힌다.

## 최근 변화

- **다른 문서 읽힘 확인** → 뜻이 다른 유효 문서에서 지도·상세·맥락 전달·다시 읽기가 그대로 됨을 확인하고 막는 결함 없이 닫힘.
- **지도에 현재 위치 하나만 남기기** → 지도에서 지금 위치 표시를 하나로 줄이고 나머지는 프로젝트 말 그대로 보여주게 됨.
- **낯선 독자 이해 확인** → 주 화면만으로 뜻·위치·다음·제약이 되살아남을 확인하고 막는 결함 없이 닫힘.
- **질문당 한 섹션으로 모으기** → 같은 질문에 답하는 겹친 부분을 없애고 한 질문에 한 섹션만 남김.
- **지도를 첫 화면 기준으로** → 지도가 첫 화면 기준이 되고 개요가 평문으로 읽힘.
- **맥락 전달로 모으기** → 프로젝트 내용 전달로 모으고 실행 절차는 저장소 계약에 맡김.

## 프로젝트 지도

### 로컬에서 읽고 확인하는 흐름
#### 이미 되는 읽기
- **로컬에서 PROGRESS.md 열어 보여주기** — 지정한 문서 하나를 로컬에서 읽어 브라우저에 보여주는 실행 부분
- **사람이 쓴 글을 지도·상세로 나누어 읽기** — 사람이 쓴 Markdown을 지도·영역·개요로 나누어 읽는 해석 부분
- **낯선 독자가 이해했는지 확인한 기록** — 첫 화면만으로 뜻을 되찾았음을 받아들인 확인
- **다른 문서에서도 첫 화면 그대로 읽기** — 뜻이 다른 유효 문서에서도 지도 우선으로 읽히는 이식 부분

#### 현재 단계
- **지도를 먼저 보여주는 첫 화면** — 지도를 먼저 보여주고 현황·다음·제약으로 방향을 알려주는 화면
- **영역을 눌러 근거까지 내려가는 상세** — 고른 영역의 뜻·현재·남은 문제·근거를 파고드는 상세 화면

## 영역 상세

### 로컬에서 PROGRESS.md 열어 보여주기
#### 의미
지정한 `PROGRESS.md` 하나와 다 지어진 화면을 로컬 되돌림 주소에서 읽어 보여주는 실행 범위이다.
#### 현재 수준
로컬·읽기 전용·결정론적으로 같은 파일을 다시 읽어 보여주는 상태로 굳어져 있다.
#### 근거
- `scripts/serve.mjs`가 문서 하나를 서빙하고 파일이 바뀌면 같은 화면을 다시 그리는 경계.

### 사람이 쓴 글을 지도·상세로 나누어 읽기
#### 의미
사람이 쓴 Markdown을 지도·영역·평문 개요로 나누는 해석기이다.
#### 현재 수준
지도·영역·개요를 하나로 묶어 읽는 구조를 표현할 수 있다.
#### 근거
- `src/parser.ts` 겉면과 해석기 회귀 예제.

### 지도를 먼저 보여주는 첫 화면
#### 의미
지도를 먼저 보여주고 현황·다음·제약 개요로 방향을 전달하는 화면이다.
#### 현재 수준
지도 우선 틀을 브라우저에 그대로 그린다.
#### 근거
- `src/main.ts` 지도 우선 뼈대와 `index.html` 지도 우선 자리.

### 영역을 눌러 근거까지 내려가는 상세
#### 의미
영역의 의미·현재 수준·남은 문제·근거를 파고드는 상세 화면이다.
#### 현재 수준
영역에서 근거로 내려가기와 영역 맥락 전달을 돕는다.
#### 근거
- `index.html`의 상세 뼈대와 영역 카드 누름길.

### 다른 문서에서도 첫 화면 그대로 읽기
#### 의미
뜻이 다른 유효 `PROGRESS.md`를 지도 우선으로 읽는 이식 범위이다.
#### 현재 수준
뜻이 다른 유효 문서에서 해석·지도·상세·맥락 전달·다시 읽기가 그대로 됨이 확인되어 닫혔다. 넓은 운영 이식은 주장하지 않으며 막는 제품 결함은 없었다.
#### 근거
- 실제 프로젝트 문서·연구 문서·최소 표준 예제에서 첫 화면만으로 핵심 상태·다음·제약을 되찾고 지도 우선·프로젝트 말·단일 현재 위치·상세 파고들기·맥락 전달·다시 읽기 선택 유지를 확인함.
- 세 문서의 구조 검사가 모두 통과했고 해석·보여주기·화면·실행 차원의 막는 인과 결함은 없었음.
- 실제 살아 있는 저장소 확인은 한 문서였고 나머지는 유효 본보기이므로 넓은 운영 이식까지 주장하지 않음.

### 낯선 독자가 이해했는지 확인한 기록
#### 의미
화면에 보이는 내용만으로 전체 뜻을 되찾는지 확인한 기록이다.
#### 현재 수준
바뀐 첫 화면에서 통과해 확정되었다.
#### 근거
- `tests/fixtures/nextchart-emr.md`와 `tests/fixtures/cockpit-self.md`의 바뀐 틀 확인용 본보기.

## 제품 목표

신뢰 가능한 `PROGRESS.md`를 처음 보는 독자에게 짧고 결정론적으로 보여주는 초경량 로컬 뷰어다.

## 확정된 방향

- Cockpit은 읽기 전용 화면이며 `PROGRESS.md`를 추론하거나 수정하지 않는다.
- 질문 하나에 섹션 하나: 같은 질문에 답하는 겹 소유를 만들지 않는다.
- 프로젝트 지도를 기준으로 유지하며, 지도와 영역 상세 대응을 결정론적으로 보존한다.
- 상세 정보 창은 개요 → 영역 → 근거로 깊어지는 보조 화면이다.
- 우선 과제는 사용자 소유이고 실행 작업과 합치지 않는다.
- 맥락 전달은 프로젝트 내용만 소유하고, 실행·발행·Git 절차는 저장소 자체 계약에 위임한다.
